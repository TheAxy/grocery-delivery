import { observer } from 'mobx-react-lite'
import { ProfilePage } from '@grocery-delivery/app-core'

const ProfilePageRemote = observer(function ProfilePageRemote() {
  return <ProfilePage />
})

export default ProfilePageRemote
